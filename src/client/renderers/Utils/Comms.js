// comms scripts are stored an UINT8 so we have 0-255 possible scripts,
// which are named here
const scripts = ['station', 'ship'];

export default class Comms {

	openComms(playerShip, obj) { // what if another ship opens comms?
	}

	respond(playerShip, obj, response) {
	}

	closeComms(playerShip, obj) {
	}

}